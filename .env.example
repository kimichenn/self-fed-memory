OPENAI_API_KEY=sk-REPLACE_ME
PINECONE_API_KEY=pcn-REPLACE_ME

LANGSMITH_TRACING="true"
LANGSMITH_API_KEY="lsv2_REPLACE_ME"
LANGSMITH_PROJECT="self_memory"

PINECONE_ENV=us-east-1
PINECONE_INDEX=self-memory
PINECONE_NAMESPACE=self-memory-namespace
EMBEDDING_MODEL=text-embedding-3-large

TEST_PINECONE_INDEX=self-memory-test
TEST_EMBEDDING_MODEL=text-embedding-3-large
TEST_PINECONE_NAMESPACE=self-memory-test-namespace

# Supabase (server-side only, single-user)
# IMPORTANT:
# - Use your SERVICE-ROLE key here (never expose it to browsers/clients)
# - Keep this out of git and hosted publicly (use environment secrets)
# - If you ever need to use the anon/publishable key, ONLY do so in the browser
#   with Row Level Security (RLS) policies enforced to restrict access.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=sb_service_role_REPLACE_ME
SUPABASE_TABLE_PREFIX=
TEST_SUPABASE_TABLE_PREFIX=test_
SUPABASE_DEFAULT_SESSION_ID=00000000-0000-0000-0000-000000000001

# Optional API key to protect selected backend endpoints (x-api-key header)
API_AUTH_KEY=
